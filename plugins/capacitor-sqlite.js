// import {
//   defineCustomElements as jeepSqlite,
//   applyPolyfills,
// } from "jeep-sqlite/loader";
// import { useSQLite } from "vue-sqlite-hook";

// applyPolyfills().then(() => {
//   jeepSqlite(window);
// });

// export default ({ app }, inject) => {
//   // Inject $hello(msg) in Vue, context and store.
//   inject("sqlite", useSQLite());
// };
